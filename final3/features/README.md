# features

Feature-facing helpers live here. The active answer-aware feature
implementation is vendored under:

```text
final3/vendor/prefix_predict_model_holdout_answer/
```

`answer.py` exposes a small final3 wrapper for gold-answer enrichment while
keeping the full implementation in the vendored source tree.

